---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-12.04
    driver_config:
      network:
      - [private_network, ip: 33.33.33.10]

suites:
  - name: knockd
    run_list:
      - recipe[fake::default]
    attributes:
      knockd:
        interface: eth1
